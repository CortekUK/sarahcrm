# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.next
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment
.env
.env.local
.vercel
.env*.local

# Secrets — Google service-account keys (NEVER commit)
*.json.key
*service-account*.json
the-club-by-sarah-restrick-*.json
gcp-*.json
credentials*.json

# Tooling / build artifacts
.claude/
tsconfig.tsbuildinfo

# Large source media — the actual hero video is hosted on Cloudinary.
# Keep raw uploads out of the repo to avoid 99MB blobs in git history.
public/hero-video.mp4
